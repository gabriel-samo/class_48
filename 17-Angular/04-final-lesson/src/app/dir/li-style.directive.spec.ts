import { LiStyleDirective } from './li-style.directive';

describe('LiStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new LiStyleDirective();
    expect(directive).toBeTruthy();
  });
});
